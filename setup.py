
from setuptools import setup

setup(name='kernelflow',
      version='0.1',
      description='Kernels and Histograms for TensorFlow',
      author='Pablo de Castro',
      packages=['kernelflow'],
      zip_safe=False)
